name: self-contained

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: No foreign plugin-namespace references in skills
        run: ./scripts/check-self-contained.sh
